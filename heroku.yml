# heroku.yml
# This file tells Heroku how to build and run your application.
setup:
  config:
    
build:
  docker:
    web: Dockerfile

release:

run:
  web:
    command:
      - streamlit run app.py --server.port=$PORT --server.enableCORS=false --server.headless=true
